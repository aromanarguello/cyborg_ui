import React, { SFC } from 'react'
import { CardContainer, Card } from '../Cards.styles'

const ProfileCard: SFC<any> = () => (
    <CardContainer width={500}>
        <Card />
    </CardContainer>
)

export default ProfileCard
